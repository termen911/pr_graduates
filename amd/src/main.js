import consent_change from 'local_pr_graduates/functions/consent_change';
import autosize from 'local_pr_graduates/functions/autosize';

export const init = () => {
    consent_change.init();
    autosize.init();
};